'use client';

import {FormContainer, TextFieldElement} from 'react-hook-form-mui';

export default function JoinPage() {
    return (
        <div>
            <FormContainer>
            </FormContainer>
        </div>
    );
}